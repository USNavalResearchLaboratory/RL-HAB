from distutils.core import setup
from setuptools import find_packages

# pip3 install -e .

setup(
    name='RLHAB',
    version='1.1.0',
    packages=find_packages(),
    install_requires=[],
)
